apiVersion: v1
kind: Service
metadata:
  name: {{ include "coin-recomender.fullname" . }}
  labels:
    {{- include "coin-recomender.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coin-recomender.selectorLabels" . | nindent 4 }}
